# Dependencias del contenedor freqtrade-mcp (tool-only wrapper)
# Instalar en un venv limpio:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# IMPORTANTE en Windows: unset PYTHONPATH antes de usar el venv (el global de Hermes contamina).

mcp>=1.27,<2.0     # FastMCP (pin rango: 2.x removió mcp.server.fastmcp)
httpx>=0.27
