feat: gateway MCP universal HTTP (conector para cualquier agente/LLM)

- servers/gateway_http.py: un solo proceso FastMCP con transporte Streamable
  HTTP (spec 2025-03-26) que agrupa toda la base de conocimiento y herramientas:
  * docs_* (5): listar_bloques, health, buscar, preguntar, buscar_global (RAG 12 bloques)
  * bot_* (9): ping, status, profit, balance, whitelist, blacklist, count, logs,
    show_config (lectura de freqtrade)
  * grafo_* (2): estadisticas, god_nodes, consultar (grafo del codigo)
  * plan_mision, cadena_dry_run (orquestacion)
  Total 19 tools en modo solo-lectura.
- SEGURIDAD: modo SOLO-LECTURA por defecto; las tools de escritura (entrar,
  salir, vetar, detener_compras) NO se registran salvo --permitir-escritura
  (autorizacion humana explicita al arrancar). Verificado por test.
- Escucha en 0.0.0.0:8765 (LAN) para servir a cualquier equipo/agente.
- tests/test_gateway.py (8): incluye verificacion critica de que en solo-lectura
  las tools de escritura NO existen.
- tools/cliente_gateway.py: cliente MCP HTTP de prueba.
- scripts/arrancar_gateway.sh: arranque en background (solo-lectura/escritura).
- docs/conector-universal.md: arquitectura, uso, ejemplos, seguridad, verificado.

Verificado end-to-end: handshake OK, 19 tools, RAG (configuracion 118 chunks
Gemini), grafo (468 nodos/939 edges), sin tools de escritura expuestas.
Suite completa: 56/56 tests GREEN.
